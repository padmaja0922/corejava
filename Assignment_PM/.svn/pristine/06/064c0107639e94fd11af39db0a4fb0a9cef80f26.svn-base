/**
 * 
 */
package com.bcj.corejava.classes.medium.lab7;

/**
 * @author Boot Camp User 008
 *
 */
public class Checks {
	

// this method return true if baggage amount is between 0-40 else returns false	
	public static boolean checkBaggage (Traveller tr ){
	
	if(tr.getBaggageAmount()>= 0 && tr.getBaggageAmount()<=40 )
		return true;
	else	
	    return false;	
}

// this method checks Expire year is between 2001 - 2025
	
public static boolean checkImmgigration(Traveller tr){
	
	if(tr.getExpiryYear() >=2001 && tr.getExpiryYear()<=2025)
	 return true;
	else
		return false;	
}

public static boolean checkSecurity(Traveller tr){
	if(tr.getNocStatus()== true)
		return true;
	else
		return false;
}


}
